<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".EMPlayApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:replace="android:allowBackup"
        tools:ignore="NotificationPermission"
        android:icon="@drawable/ic_logo_punch_e"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_punch_e"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMPlay"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:theme="@style/HiddenTitleTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity android:name=".activity.TrailerActivity"
            android:theme="@style/HiddenTitleTheme">
        </activity>
        <activity android:name=".activity.AboutActivity"
            android:theme="@style/HiddenTitleTheme"/>

        <!-- Receives the deep-link redirect after TMDB OAuth approval -->
        <activity
            android:name=".activity.TMDBAuthActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/HiddenTitleTheme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="emplay"
                    android:host="auth"
                    android:path="/tmdb" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>