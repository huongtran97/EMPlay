<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="88dp">

    <!-- Poster -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/search_poster_card"
        android:layout_width="56dp"
        android:layout_height="84dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        app:cardCornerRadius="6dp"
        app:cardElevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/search_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/show_poster"
            android:scaleType="centerCrop" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Type chip -->
    <TextView
        android:id="@+id/tvTypeChip"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        android:layout_marginStart="12dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_type_badge"
        android:fontFamily="@font/inter"
        android:gravity="center"
        android:paddingHorizontal="6dp"
        android:textAllCaps="true"
        android:textColor="@color/accent"
        android:textSize="@dimen/text_tiny"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/search_poster_card"
        app:layout_constraintTop_toTopOf="@id/search_poster_card"
        tools:text="MOVIE" />

    <!-- Title -->
    <TextView
        android:id="@+id/search_title"
        style="@style/TextAppearance.EMPlay.Title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="12dp"
        android:ellipsize="end"
        android:maxLines="2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/search_poster_card"
        app:layout_constraintTop_toBottomOf="@id/tvTypeChip"
        tools:text="The Dark Knight" />

    <!-- Year -->
    <TextView
        android:id="@+id/search_release_date"
        style="@style/TextAppearance.EMPlay.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="6dp"
        app:layout_constraintStart_toEndOf="@id/search_poster_card"
        app:layout_constraintTop_toBottomOf="@id/search_title"
        tools:text="2024" />

    <!-- Rating star -->
    <ImageView
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginStart="8dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_star"
        app:layout_constraintBottom_toBottomOf="@id/search_release_date"
        app:layout_constraintStart_toEndOf="@id/search_release_date"
        app:layout_constraintTop_toTopOf="@id/search_release_date"
        app:tint="@color/accent" />

    <!-- Rating value -->
    <TextView
        android:id="@+id/tvPosterRating"
        style="@style/TextAppearance.EMPlay.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        app:layout_constraintBottom_toBottomOf="@id/search_release_date"
        app:layout_constraintStart_toEndOf="@id/search_release_date"
        app:layout_constraintTop_toTopOf="@id/search_release_date"
        tools:text="8.5" />

    <!-- Hidden compat field used by legacy BaseSearchAdapter -->
    <TextView
        android:id="@+id/search_language"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <!-- Hairline divider -->
    <View
        android:layout_width="0dp"
        android:layout_height="@dimen/hairline"
        android:background="@color/hairline_row"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>