<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.MainActivity">

    <!--
        Full-height container: content intentionally extends behind the floating nav bar.
        The glass effect blurs whatever is rendered behind the nav bar.
        Fragments handle their own bottom insets (status bar + system nav via fitsSystemWindows
        or ViewCompat.setOnApplyWindowInsetsListener).
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/movie_bottom_navigation_view"
        style="@style/Widget.EMPlay.BottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/bg_bottom_nav_glass"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        app:elevation="0dp"
        app:menu="@menu/movie_bottom_navigate_menu"
        app:itemRippleColor="@null"
        app:layout_behavior="com.google.android.material.behavior.HideBottomViewOnScrollBehavior" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>