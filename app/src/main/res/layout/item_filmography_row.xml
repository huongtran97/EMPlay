<?xml version="1.0" encoding="utf-8"?>
<!-- EMPlay · ITEM: Filmography Row (used in Cast screen)
     Gold serif year | Title + role line | Star rating -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="13dp"
    android:paddingTop="9dp"
    android:paddingEnd="13dp"
    android:paddingBottom="9dp">

    <!-- Year (gold, serif) -->
    <TextView
        android:id="@+id/tv_year"
        android:layout_width="34dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/playfair_display"
        android:text="2026"
        android:textColor="@color/accent"
        android:textSize="@dimen/text_divider_label" />

    <!-- Title + role -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_film_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/inter_semibold"
            android:text="Dune: Part Three"
            android:textColor="@color/text_1"
            android:textSize="@dimen/text_divider_label" />

        <TextView
            android:id="@+id/tv_film_role"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="as Chani · Movie"
            android:textColor="@color/text_3"
            android:textSize="@dimen/text_badge" />

    </LinearLayout>

    <!-- Rating -->
    <TextView
        android:id="@+id/tv_film_rating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="★ 8.2"
        android:textColor="@color/accent"
        android:textSize="@dimen/text_badge" />

</LinearLayout>